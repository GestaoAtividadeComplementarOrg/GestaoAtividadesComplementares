spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false

# Test-only secret (not used in production). This file lives on the test
# classpath so it takes precedence over src/main/resources/application-test.properties,
# which is why the H2 settings above are duplicated here rather than relying on merging.
jwt.secret=7hlh3JJwQOSoWiU8L5XwEDxE2PfkafFBgyr7lQaEb78=
